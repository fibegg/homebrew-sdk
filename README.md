# fibegg/homebrew-sdk

Homebrew tap for the Fibe CLI.

## Install

```bash
brew install fibegg/sdk/fibe
```

Or:

```bash
brew tap fibegg/sdk
brew install fibe
```

## Maintenance

The `Formula/fibe.rb` file is published automatically by GoReleaser from tags in `fibegg/sdk`.
